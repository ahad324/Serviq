import 'package:flutter/material.dart';

class AppColors {
  // Primary brand color
  static const Color primary = Color(0xFF004643); // Cyprus
  static const Color primaryLight = Color(0xFF006B67);
  static const Color primaryDark = Color(0xFF002E2C);

  // Background colors
  static const Color background = Color(0xFFF0EDE5); // Sand
  static const Color surface = Colors.white;
  static const Color surfaceDark = Color(0xFFE5E2D8);

  // Text colors
  static const Color textPrimary = Color(0xFF1A1C1B);
  static const Color textSecondary = Color(0xFF5E6261);
  static const Color textDisabled = Color(0xFF9CA3AF);

  // Status colors
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  // Accent colors
  static const Color accent = Color(0xFFF9BC60); // Mustard/Amber accent
}
